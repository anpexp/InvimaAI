# URL del Orquestador Principal (ver ../orquestador). El fetch corre en el
# navegador del usuario, así que debe ser una URL alcanzable desde ahí
# (no un hostname interno de Docker).
VITE_ORQUESTADOR_URL=http://localhost:8080
